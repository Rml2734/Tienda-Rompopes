{
  "name": "tienda-rompopes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
  "build": "vite build", 
  "preview": "vite preview", 
  "start": "vite preview --host --port 8080 --no-open"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "rollup": "3.29.4",
    "serve": "^14.2.5",
    "vite": "^4.5.3"
  },
  "dependencies": {
    "@stripe/stripe-js": "^7.9.0"
  },
  "engines": {
    "node": "20.18.1"
  }
}
